#include "model.h"

Model::Model()
{
    /*
    //globals
    double C_A;           // anterior discrete diffusion parameter
    double C_P;           // posterior discrete diffusion parameter
    double C_alpha;       // segmentation discrete diffusion parameter
    double C;             // axial segmentation clock
    double dC;            // clock derivative
    double C_BAM;         // anterior border discrete diffusion parameter
    double C_BPM;         // posterior border discrete diffusion parmeter
    double recip_e;       // 1 / e
    bool recording_movie; // set if recording a movie

    //patches-own
    double A, Ap;         // anterior morphogen
    double P, Pp;         // posterior morphogen
    double M, Mp;         // cell mass
    double S, Sp;         // somite concentration
    double T, Tp;         // tail cell concentration
    double G, Gp;         // growth signal
    double K;             // axial clock signal
    double dK;            // derivative of axial clock signal
    double L;             // peripheral clock signal
    double dL;            // derivative of peripheral clock signal
    double phi;           // firing signal
    double alpha, alphap; // segmentation signal
    double rho, rhop;     // recovery variable
    double Vx, Vy;        // velocity
    double BA, BAp;       // anterior border marker
    double BP, BPp;       // posterior border marker
    double AB;            // anterior morphogen block
    double BAM, BAMp;     // anterior border morphogen
    double BPM, BPMp;     // posterior border morphogen
    double ID;            // imaginal disk for limb (called K in user interface)
    double dID;           // derivative of ID density
    double local_density; // local S (somite) density
    double edge;          // edge signal
    double LM;            // limb marker concentration
    */
}
